import { formatter } from "./formatter/formatter";

/** @deprecated use formatter.bytes */
export const formatBytes = formatter.bytes;

/** @deprecated use formatter.speed */
export const formatSpeed = formatter.speed;
